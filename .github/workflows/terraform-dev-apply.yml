name: Terraform - dev (apply)

# Manual only. Applies the exact plan produced for the given PR by
# "Terraform - dev (plan)" - review that plan (in the PR comment or
# the plan run's summary) before running this. The `dev` GitHub
# Environment's required reviewers gate the actual apply step (see
# _terraform-apply-reusable.yml).

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number whose reviewed plan should be applied"
        required: true

jobs:
  apply:
    uses: ./.github/workflows/_terraform-apply-reusable.yml
    with:
      environment: dev
      pr_number: ${{ inputs.pr_number }}
      plan_workflow_file: terraform-dev-plan.yml
    secrets: inherit
